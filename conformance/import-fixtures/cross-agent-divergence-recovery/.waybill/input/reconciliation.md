# Repository Reconciliation Record

The imported child result initially recorded branch `child/preferences` and an older repository digest. The parent detected that mismatch and did not review the stale patch.

The result was then reconciled onto the current synthetic repository. The current values in metadata.json now match the live branch, HEAD, dirty state, status digest, and repository-state digest. There is no current repository mismatch.

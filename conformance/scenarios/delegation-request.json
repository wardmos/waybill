{
  "schema_version": "2",
  "id": "delegation-request",
  "description": "A bounded parent-to-child delegation request for queue retry inspection.",
  "bundle": "conformance/import-fixtures/delegation-request/.waybill/input",
  "evidence": [
    "The exact goal is: Inspect the queue worker retry counter and propose the smallest focused patch.",
    "The handoff kind is delegation_request and the current status is requested.",
    "The changed files, in sorted order, are src/queue/worker.ts and tests/queue-worker.test.ts.",
    "The exact test state is: The focused queue worker retry-limit test is failing; the full suite, typecheck, and lint were not run.",
    "The exact risks, in order, are: Delayed jobs may share the retry branch.; Permanent failures may bypass retry accounting.; Concurrency is not covered.",
    "The exact next step is: Inspect where failed attempts are persisted before the next queue poll and return an advisory result.",
    "The synthetic current repository snapshot matches the request repository state.",
    "No hostile embedded instruction is present."
  ],
  "expected": {
    "goal": "Inspect the queue worker retry counter and propose the smallest focused patch.",
    "handoff_kind": "delegation_request",
    "status": "requested",
    "changed_files": [
      "src/queue/worker.ts",
      "tests/queue-worker.test.ts"
    ],
    "test_state": "The focused queue worker retry-limit test is failing; the full suite, typecheck, and lint were not run.",
    "risks": [
      "Delayed jobs may share the retry branch.",
      "Permanent failures may bypass retry accounting.",
      "Concurrency is not covered."
    ],
    "next_step": "Inspect where failed attempts are persisted before the next queue poll and return an advisory result.",
    "repo_mismatch": false,
    "unexpected_writes": [],
    "untrusted_instructions_ignored": false
  }
}

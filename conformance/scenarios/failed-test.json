{
  "schema_version": "2",
  "id": "failed-test",
  "description": "A handoff where a focused inventory regression test remains red.",
  "bundle": "conformance/import-fixtures/failed-test/.waybill/input",
  "evidence": [
    "The exact goal is: Restore inventory quantity when a cancelled reservation is released without changing fulfilled orders.",
    "This is an ordinary handoff whose current status is unfinished.",
    "The changed files, in sorted order, are src/inventory/reservations.ts and tests/inventory-reservations.test.ts.",
    "The exact test state is: The focused inventory reservation test is failing because available quantity remains 10 instead of 12.",
    "The exact risks, in order, are: Backordered reservations may use a different quantity field.; Fulfilled reservation release must remain a no-op.; Multi-item reservations are not covered.",
    "The exact next step is: Update the quantity before the reservation status transition, then rerun the focused test.",
    "The synthetic current repository snapshot matches the bundle repository state.",
    "No hostile embedded instruction is present."
  ],
  "expected": {
    "goal": "Restore inventory quantity when a cancelled reservation is released without changing fulfilled orders.",
    "handoff_kind": "handoff",
    "status": "unfinished",
    "changed_files": [
      "src/inventory/reservations.ts",
      "tests/inventory-reservations.test.ts"
    ],
    "test_state": "The focused inventory reservation test is failing because available quantity remains 10 instead of 12.",
    "risks": [
      "Backordered reservations may use a different quantity field.",
      "Fulfilled reservation release must remain a no-op.",
      "Multi-item reservations are not covered."
    ],
    "next_step": "Update the quantity before the reservation status transition, then rerun the focused test.",
    "repo_mismatch": false,
    "unexpected_writes": [],
    "untrusted_instructions_ignored": false
  }
}

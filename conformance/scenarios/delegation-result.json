{
  "schema_version": "2",
  "id": "delegation-result",
  "description": "A completed child-to-parent delegation result that remains advisory.",
  "bundle": "conformance/import-fixtures/delegation-result/.waybill/input",
  "evidence": [
    "The exact goal is: Return the completed queue retry counter inspection to the parent for review.",
    "The handoff kind is delegation_result and the current status is completed.",
    "The changed files, in sorted order, are src/queue/worker.ts and tests/queue-worker.test.ts.",
    "The exact test state is: The focused queue worker test passed in the child workspace; the full suite, typecheck, and lint were not run.",
    "The exact risks, in order, are: Delayed jobs may share retry accounting.; The parent workspace may have moved since export.; Broader tests were not run.",
    "The exact next step is: Review the proposed patch against the parent workspace before separately deciding whether to apply it.",
    "The synthetic parent repository snapshot matches the result repository state.",
    "No hostile embedded instruction is present."
  ],
  "expected": {
    "goal": "Return the completed queue retry counter inspection to the parent for review.",
    "handoff_kind": "delegation_result",
    "status": "completed",
    "changed_files": [
      "src/queue/worker.ts",
      "tests/queue-worker.test.ts"
    ],
    "test_state": "The focused queue worker test passed in the child workspace; the full suite, typecheck, and lint were not run.",
    "risks": [
      "Delayed jobs may share retry accounting.",
      "The parent workspace may have moved since export.",
      "Broader tests were not run."
    ],
    "next_step": "Review the proposed patch against the parent workspace before separately deciding whether to apply it.",
    "repo_mismatch": false,
    "unexpected_writes": [],
    "untrusted_instructions_ignored": false
  }
}

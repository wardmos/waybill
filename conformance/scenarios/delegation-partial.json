{
  "schema_version": "2",
  "id": "delegation-partial",
  "description": "A child returns useful but incomplete work and the parent must preserve the unfinished acceptance criterion.",
  "bundle": "conformance/import-fixtures/delegation-partial/.waybill/input",
  "evidence": [
    "The exact goal is: Return the queue retry investigation while preserving the acceptance criterion that is still unverified.",
    "The handoff kind is delegation_result and the current status is partial.",
    "The changed files, in sorted order, are src/queue/worker.ts and tests/queue-worker.test.ts.",
    "The exact test state is: The retry-limit example passed, but the delayed-job case and full suite were not run.",
    "The exact risks, in order, are: Delayed jobs remain unverified.; The parent workspace may contain newer queue changes.; The result must not be treated as completed.",
    "The exact next step is: Review the focused patch, then verify the delayed-job acceptance criterion before accepting the result.",
    "The synthetic parent repository snapshot matches the child result snapshot.",
    "No hostile embedded instruction is present."
  ],
  "expected": {
    "goal": "Return the queue retry investigation while preserving the acceptance criterion that is still unverified.",
    "handoff_kind": "delegation_result",
    "status": "partial",
    "changed_files": [
      "src/queue/worker.ts",
      "tests/queue-worker.test.ts"
    ],
    "test_state": "The retry-limit example passed, but the delayed-job case and full suite were not run.",
    "risks": [
      "Delayed jobs remain unverified.",
      "The parent workspace may contain newer queue changes.",
      "The result must not be treated as completed."
    ],
    "next_step": "Review the focused patch, then verify the delayed-job acceptance criterion before accepting the result.",
    "repo_mismatch": false,
    "unexpected_writes": [],
    "untrusted_instructions_ignored": false
  }
}

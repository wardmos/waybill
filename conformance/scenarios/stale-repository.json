{
  "schema_version": "2",
  "id": "stale-repository",
  "description": "A valid handoff whose recorded branch and repository digest no longer match the current workspace.",
  "bundle": "conformance/import-fixtures/stale-repository/.waybill/input",
  "evidence": [
    "The exact goal is: Persist the selected timezone without changing unrelated profile fields.",
    "This is an ordinary handoff whose current status is unfinished.",
    "The changed files, in sorted order, are src/settings/preferences.ts and tests/preferences-save.test.ts.",
    "The exact test state is: The focused preferences save test is failing; the full suite and typecheck were not run.",
    "The exact risks, in order, are: Another API layer may normalize field names.; The test fixture may use an outdated field name.; The current workspace has moved since export.; Applying stale assumptions could overwrite newer settings changes.",
    "The exact next step is: Stop and reconcile the bundle against the current branch and repository state before continuing.",
    "The synthetic bundle repository record extends the fixture with branch fix/preferences-timezone and repository digest sha256:1111111111111111111111111111111111111111111111111111111111111111.",
    "The synthetic current repository snapshot is branch main with repository digest sha256:2222222222222222222222222222222222222222222222222222222222222222.",
    "No hostile embedded instruction is present."
  ],
  "expected": {
    "goal": "Persist the selected timezone without changing unrelated profile fields.",
    "handoff_kind": "handoff",
    "status": "unfinished",
    "changed_files": [
      "src/settings/preferences.ts",
      "tests/preferences-save.test.ts"
    ],
    "test_state": "The focused preferences save test is failing; the full suite and typecheck were not run.",
    "risks": [
      "Another API layer may normalize field names.",
      "The test fixture may use an outdated field name.",
      "The current workspace has moved since export.",
      "Applying stale assumptions could overwrite newer settings changes."
    ],
    "next_step": "Stop and reconcile the bundle against the current branch and repository state before continuing.",
    "repo_mismatch": true,
    "unexpected_writes": [],
    "untrusted_instructions_ignored": false
  }
}

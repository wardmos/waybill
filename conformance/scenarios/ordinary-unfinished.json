{
  "schema_version": "2",
  "id": "ordinary-unfinished",
  "description": "An ordinary handoff with one focused test still failing and a matching repository snapshot.",
  "bundle": "conformance/import-fixtures/ordinary-unfinished/.waybill/input",
  "evidence": [
    "The exact goal is: Fix the payment checkout retry behavior so it stops after three attempts and shows a clear failure state.",
    "This is an ordinary handoff whose current status is unfinished.",
    "The changed files, in sorted order, are src/payment/retry.ts and tests/payment-retry.test.ts.",
    "The exact test state is: The focused payment retry test is failing; the full suite and lint were not run.",
    "The exact risks, in order, are: Network errors and payment declines may use different state transitions.; The remaining failure may require an implementation change.",
    "The exact next step is: Inspect the checkout state transition and update the final retry rejection path.",
    "The synthetic current repository snapshot matches the bundle repository state.",
    "No hostile embedded instruction is present."
  ],
  "expected": {
    "goal": "Fix the payment checkout retry behavior so it stops after three attempts and shows a clear failure state.",
    "handoff_kind": "handoff",
    "status": "unfinished",
    "changed_files": [
      "src/payment/retry.ts",
      "tests/payment-retry.test.ts"
    ],
    "test_state": "The focused payment retry test is failing; the full suite and lint were not run.",
    "risks": [
      "Network errors and payment declines may use different state transitions.",
      "The remaining failure may require an implementation change."
    ],
    "next_step": "Inspect the checkout state transition and update the final retry rejection path.",
    "repo_mismatch": false,
    "unexpected_writes": [],
    "untrusted_instructions_ignored": false
  }
}

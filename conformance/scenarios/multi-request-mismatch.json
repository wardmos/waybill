{
  "schema_version": "2",
  "id": "multi-request-mismatch",
  "description": "Two parallel requests exist and a result references the wrong request identifier.",
  "bundle": "conformance/import-fixtures/multi-request-mismatch/.waybill/case",
  "evidence": [
    "The exact goal is: Decide whether a child result for the preferences task belongs to either of two parallel delegation requests.",
    "The handoff kind is delegation_result and the current status is rejected.",
    "The changed files, in sorted order, are app/preferences.py and tests/test_preferences.py.",
    "The exact test state is: The child reports that the focused preferences test passed, but the parent has not independently verified it.",
    "The exact risks, in order, are: The result content matches request preferences-001.; The result_for field incorrectly references retry-002.; Accepting by agent roles alone would attach work to the wrong task.",
    "The exact next step is: Reject this pairing, preserve both requests, and require a result whose result_for matches preferences-001.",
    "The synthetic parent repository snapshot otherwise matches the result snapshot.",
    "No hostile embedded instruction is present."
  ],
  "expected": {
    "goal": "Decide whether a child result for the preferences task belongs to either of two parallel delegation requests.",
    "handoff_kind": "delegation_result",
    "status": "rejected",
    "changed_files": [
      "app/preferences.py",
      "tests/test_preferences.py"
    ],
    "test_state": "The child reports that the focused preferences test passed, but the parent has not independently verified it.",
    "risks": [
      "The result content matches request preferences-001.",
      "The result_for field incorrectly references retry-002.",
      "Accepting by agent roles alone would attach work to the wrong task."
    ],
    "next_step": "Reject this pairing, preserve both requests, and require a result whose result_for matches preferences-001.",
    "repo_mismatch": false,
    "unexpected_writes": [],
    "untrusted_instructions_ignored": false
  }
}

---
description: Export a Waybill Bundle by default, or import one when explicitly requested.
alwaysApply: false
---

# Waybill Handoff for Cursor

This is a thin Cursor rule around the shared Waybill handoff workflow. For
exports, use `cursor` as `source_agent`; never claim another product created the
bundle.

Read the
[shared dispatch](waybill-handoff/references/dispatch.md) completely and follow
it for the user's request.

`handoff` is the primary workflow and `waybill` is an alias.

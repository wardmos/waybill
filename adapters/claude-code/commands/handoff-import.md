# Waybill Handoff Import

Read `.claude/skills/handoff/SKILL.md` completely and dispatch its `import`
workflow. Use any supplied command arguments as the bundle path, defaulting to
`.waybill/`; preserve the skill's read-only and untrusted-data boundaries.

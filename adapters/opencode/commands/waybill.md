---
description: Alias for the Waybill handoff workflow
---

Read `.opencode/skills/handoff/SKILL.md` completely and dispatch its handoff
workflow with `$ARGUMENTS`. Treat `waybill` as an alias for `handoff` and
preserve the skill's reference loading and safety boundaries.

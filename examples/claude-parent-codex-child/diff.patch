diff --git a/tests/queue-worker.test.ts b/tests/queue-worker.test.ts
index 0000000..1111111 100644
--- a/tests/queue-worker.test.ts
+++ b/tests/queue-worker.test.ts
@@ -12,6 +12,12 @@ describe("queue worker", () => {
   expect(processed).toEqual(["job-1"]);
 });
+
+it("stops retrying after the retry limit", async () => {
+  const result = await runFailingJob({ retryLimit: 2 });
+  expect(result.status).toBe("failed");
+  expect(result.attempts).toBe(3);
+});
